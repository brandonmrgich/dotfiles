---
description: Audit a plan task against its master plan — invokes the plan-auditor skill (task-scoped).
---

## Your task

Use the plan-auditor skill to audit task: $ARGUMENTS

$ARGUMENTS should be the task file path (e.g., `tasks/05-foo.md`) and ideally the master plan path. If unclear, ask the user for both before running.
