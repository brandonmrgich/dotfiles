---
description: Probe whether a fresh Claude session could pick up the current work — invokes the session-ready skill.
---

## Your task

Use the session-ready skill. Target: $ARGUMENTS (or infer current work if empty).

Dispatch a fresh-context probe and report orientation/next-action/gaps.
